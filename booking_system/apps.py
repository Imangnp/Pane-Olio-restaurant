from django.apps import AppConfig


# Defines the configuration for the 'booking_system' app
class BookingSystemConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'booking_system'
